<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sara.navigation">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/notifiche"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".VisualizzaRisorse"
            android:label="@string/risorse" />
        <activity
            android:name=".EditActivity"
            android:label="@string/risorse" />
        <activity android:name=".AddActivity" />
        <activity
            android:name=".Modifica"
            android:label="@string/risorse" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".Meteo"
            android:label="@string/meteo" />
        <activity
            android:name=".AddActivity1"
            android:label="@string/interventi" />
        <activity
            android:name=".EditActivity1"
            android:label="@string/interventi" />
        <activity android:name=".MapsActivity" />
        <activity
            android:name=".TabellaInterventi"
            android:label="@string/interventi" />
        <activity
            android:name=".MainActivity1"
            android:label="@string/interventi" />
        <activity
            android:name=".MainActivityAvvisi"
            android:label="@string/avvisi" />
        <activity
            android:name=".EditActivityAvvisi"
            android:label="@string/avvisi" />
        <activity
            android:name=".AddActivityAvvisi"
            android:label="@string/avvisi" />
        <activity
            android:name=".MainActivityRegistro"
            android:label="@string/registro" />
        <activity android:name=".NotificheVolontario" />
        <activity android:name=".NotificheSindaco" />
        <activity android:name=".Meteo2"></activity>
    </application>

</manifest>